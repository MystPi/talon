fun vector(x, y, z) {
  ret [x, y, z]
}

fun magnitude(v) {
  ret sqrt(v[0] ^ 2 + v[1] ^ 2 + v[2] ^ 2)
}

fun dot(v1, v2) {
  ret v1[0] * v2[0] + v1[1] * v2[1] + v1[2] * v2[2]
}

this v1 = vector(1, 2, 3)
this v2 = vector(2, 3, 4)

print(magnitude(v1), magnitude(v2))
print(dot(v1, v2))